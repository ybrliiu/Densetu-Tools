{
  app => {
    admin_password => $ENV{ADMIN_PASSWORD},
    session => {
      password    => $ENV{SESSION_PASSWORD},
      cookie_name => 'densetu-tools',
      expiration  => 3600000,
    },
  }
}
